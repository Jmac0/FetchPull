const poop = (word, emoji) => {

return `${word} ${emoji} `
}

poop('you are a', emoji);

const emojis = {
 names: true,  
 icons: ['✅', '🍆', '💩', '😀'],
 somePorp: 'info'
  
  
};

const hotStrumpet = true;

